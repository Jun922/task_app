<div>
  <section>
    <%= render 'shared/stats' %>
    <% if @users.any? %>
      <div>
        <% @users.each do |user| %>
          <%= link_to user.name, user_path(user.id) %>
        <% end %>
      </div>
    <% end %>
  </section>
</div>